- name: Provision AWS server locally
  hosts: local
  tasks:
   - name: Update package list
     ansible.builtin.apt:
      update_cache: yes
      cache_valid_time: 3600

   - name: install packages
     ansible.builtin.apt:
      name:
        - nginx
        - postgresql
      state: present